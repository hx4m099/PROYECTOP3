<Window x:Class="GUI.frmReporteCompras"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sistema de Ventas - Reporte de Compras" 
        Height="700" 
        Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA">

    <!-- Recursos movidos al inicio -->
    <Window.Resources>
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              Margin="{TemplateBinding Padding}" 
                                              VerticalAlignment="Center"/>
                                <TextBlock x:Name="WatermarkText"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="#ADB5BD"
                                           Margin="{TemplateBinding Padding}"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="WatermarkText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="WatermarkText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007BFF"/>
                                <Setter Property="BorderThickness" Value="2"/>
                                <Setter TargetName="WatermarkText" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                    <Condition Property="IsFocused" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="WatermarkText" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value="{x:Null}"/>
                                    <Condition Property="IsFocused" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="WatermarkText" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox" 
                                                   Grid.Column="0" 
                                                   Margin="8,0,0,0" 
                                                   VerticalAlignment="Center"
                                                   BorderThickness="0"
                                                   Background="Transparent"/>
                                <Button x:Name="PART_Button" 
                                        Grid.Column="1" 
                                        Width="30" 
                                        Height="30"
                                        Background="Transparent" 
                                        BorderThickness="0"
                                        Content="📅"
                                        FontSize="12"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007BFF"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" 
                                                      Data="M 0 0 L 4 4 L 8 0 Z" 
                                                      Fill="#6C757D" 
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" 
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}" 
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                              Margin="{TemplateBinding Padding}" 
                                              VerticalAlignment="Center" 
                                              HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" 
                                   Placement="Bottom" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" 
                                   Focusable="False" 
                                   PopupAnimation="Slide">
                                <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}" 
                                      MinWidth="{TemplateBinding ActualWidth}">
                                    <Border Background="White" 
                                            BorderBrush="#DEE2E6" 
                                            BorderThickness="1" 
                                            CornerRadius="4">
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007BFF"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.7"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RoundButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20" 
                BorderBrush="#E9ECEF" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="🛒 Reporte de Compras" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#2C5AA0" 
                           Margin="0,0,0,5"/>
                <TextBlock Text="Genere y consulte reportes detallados de compras por rango de fechas y proveedor" 
                           FontSize="14" 
                           Foreground="#6C757D"/>
            </StackPanel>
        </Border>

        <!-- Filters Section -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20"
                BorderBrush="#E9ECEF" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="🔍 Filtros de Búsqueda" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Foreground="#495057" 
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Margin="20,0,0,0">
                    <TextBlock Text="Fecha Inicio:" FontWeight="SemiBold" Foreground="#495057" Margin="0,0,0,5"/>
                    <DatePicker x:Name="txtfechainicio" 
                                Height="40" 
                                FontSize="14"
                                BorderBrush="#DEE2E6" 
                                BorderThickness="1"
                                Style="{StaticResource ModernDatePickerStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="Fecha Fin:" FontWeight="SemiBold" Foreground="#495057" Margin="0,0,0,5"/>
                    <DatePicker x:Name="txtfechafin" 
                                Height="40" 
                                FontSize="14"
                                BorderBrush="#DEE2E6" 
                                BorderThickness="1"
                                Style="{StaticResource ModernDatePickerStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="7">
                    <TextBlock Text="Proveedor:" FontWeight="SemiBold" Foreground="#495057" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cboproveedor" 
                              Height="40" 
                              Padding="12,8" 
                              FontSize="14"
                              BorderBrush="#DEE2E6" 
                              BorderThickness="1"
                              Style="{StaticResource ModernComboBoxStyle}"/>
                </StackPanel>

                <Button Grid.Column="9" 
                        x:Name="btnbuscarresultado"
                        Content="🔍 Buscar" 
                        Background="#007BFF" 
                        Foreground="White" 
                        Height="40" 
                        Padding="20,8" 
                        BorderThickness="0" 
                        FontWeight="Bold"
                        FontSize="14"
                        Margin="0,20,0,0"
                        Cursor="Hand"
                        Click="btnbuscarresultado_Click"
                        Style="{StaticResource ActionButtonStyle}"/>
            </Grid>
        </Border>

        <!-- Search and Export Section -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20"
                BorderBrush="#E9ECEF" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="🔎 Buscar en resultados:" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Foreground="#495057" 
                           VerticalAlignment="Center"/>

                <ComboBox Grid.Column="1" 
                          x:Name="cbobusqueda" 
                          Height="35" 
                          Padding="10,6" 
                          FontSize="13"
                          BorderBrush="#DEE2E6" 
                          BorderThickness="1"
                          Margin="15,0,0,0"
                          Style="{StaticResource ModernComboBoxStyle}"/>

                <TextBox Grid.Column="3" 
                         x:Name="txtbusqueda" 
                         Height="35" 
                         Padding="10" 
                         FontSize="13"
                         BorderBrush="#DEE2E6" 
                         BorderThickness="1"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Tag="Escriba para buscar..."/>

                <Button Grid.Column="5" 
                        x:Name="btnbuscar"
                        Content="🔍" 
                        Width="35" 
                        Height="35" 
                        Background="#007BFF" 
                        Foreground="White" 
                        BorderThickness="0" 
                        FontSize="14"
                        Cursor="Hand"
                        Click="btnbuscar_Click"
                        Style="{StaticResource RoundButtonStyle}"/>

                <Button Grid.Column="7" 
                        x:Name="btnlimpiarbuscador"
                        Content="🧹" 
                        Width="35" 
                        Height="35" 
                        Background="#6C757D" 
                        Foreground="White" 
                        BorderThickness="0" 
                        FontSize="14"
                        Cursor="Hand"
                        Click="btnlimpiarbuscador_Click"
                        Style="{StaticResource RoundButtonStyle}"/>

                <Button Grid.Column="9" 
                        x:Name="btnexportar"
                        Content="📥 Descargar Excel" 
                        Background="#28A745" 
                        Foreground="White" 
                        Height="35" 
                        Padding="15,8" 
                        BorderThickness="0" 
                        FontWeight="Bold"
                        FontSize="13"
                        Cursor="Hand"
                        Click="btnexportar_Click"
                        Style="{StaticResource ActionButtonStyle}"/>
            </Grid>
        </Border>

        <!-- DataGrid Section -->
        <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="20" 
                BorderBrush="#E9ECEF" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="📋 Resultados del Reporte" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="#495057"/>
                    <TextBlock x:Name="lblTotalRegistros" 
                               Text="(0 registros)" 
                               FontSize="14" 
                               Foreground="#6C757D" 
                               Margin="10,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <DataGrid Grid.Row="1" 
                          x:Name="dgvdata" 
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="True"
                          CanUserResizeRows="False"
                          SelectionMode="Extended"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          Background="White"
                          BorderThickness="0"
                          RowBackground="White"
                          AlternatingRowBackground="#F8F9FA"
                          FontSize="12">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha Registro" Binding="{Binding FechaRegistro}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Tipo Documento" Binding="{Binding TipoDocumento}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Número Documento" Binding="{Binding NumeroDocumento}" Width="130" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Monto Total" Binding="{Binding MontoTotal, StringFormat=C}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Usuario Registro" Binding="{Binding UsuarioRegistro}" Width="130" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Documento Proveedor" Binding="{Binding DocumentoProveedor}" Width="140" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Razón Social" Binding="{Binding RazonSocial}" Width="150" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Código Producto" Binding="{Binding CodigoProducto}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Nombre Producto" Binding="{Binding NombreProducto}" Width="150" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Precio Compra" Binding="{Binding PrecioCompra, StringFormat=C}" Width="110" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Precio Venta" Binding="{Binding PrecioVenta, StringFormat=C}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Sub Total" Binding="{Binding SubTotal, StringFormat=C}" Width="100" IsReadOnly="True"/>
                    </DataGrid.Columns>

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F9FA"/>
                            <Setter Property="Foreground" Value="#495057"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="8,10"/>
                            <Setter Property="BorderBrush" Value="#DEE2E6"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>

                <!-- Summary Section -->
                <Border Grid.Row="2" 
                        Background="#F8F9FA" 
                        CornerRadius="4" 
                        Padding="15" 
                        Margin="0,15,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="💰 Total Compras: " 
                                       FontWeight="Bold" 
                                       Foreground="#495057" 
                                       FontSize="14"/>
                            <TextBlock x:Name="lblTotalCompras" 
                                       Text="$0.00" 
                                       FontWeight="Bold" 
                                       Foreground="#DC3545" 
                                       FontSize="16"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3" Orientation="Horizontal">
                            <TextBlock Text="📈 Margen Potencial: " 
                                       FontWeight="Bold" 
                                       Foreground="#495057" 
                                       FontSize="14"/>
                            <TextBlock x:Name="lblMargenPotencial" 
                                       Text="$0.00" 
                                       FontWeight="Bold" 
                                       Foreground="#28A745" 
                                       FontSize="16"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>