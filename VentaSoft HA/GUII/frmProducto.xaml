<Window x:Class="GUI.frmProducto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sistema de Ventas - Gestión de Productos" 
        Height="700" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA">

    <Window.Resources>
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007BFF"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#007BFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007BFF"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#007BFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.7"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RoundButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20" 
                BorderBrush="#E9ECEF" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="📦 Gestión de Productos" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#2C5AA0" 
                           Margin="0,0,0,5"/>
                <TextBlock Text="Administre el catálogo de productos del sistema" 
                           FontSize="14" 
                           Foreground="#6C757D"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Product Details -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="25" 
                    BorderBrush="#E9ECEF" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="📝 Detalle Producto" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="#495057" 
                                   Margin="0,0,0,20"/>

                        <!-- Hidden Fields -->
                        <TextBox x:Name="txtindice" Visibility="Hidden" Text="-1"/>
                        <TextBox x:Name="txtid" Visibility="Hidden" Text="0"/>

                        <!-- Código -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Código:" FontWeight="SemiBold" Foreground="#495057" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtcodigo" 
                                     Height="50" 
                                     Style="{StaticResource ModernTextBoxStyle}"/>
                        </StackPanel>

                        <!-- Nombre -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Nombre:" FontWeight="SemiBold" Foreground="#495057" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtnombre" 
                                     Height="48" 
                                     Style="{StaticResource ModernTextBoxStyle}"/>
                        </StackPanel>

                        <!-- Descripción -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Descripción:" FontWeight="SemiBold" Foreground="#495057" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtdescripcion" 
                                     Height="80" 
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     Style="{StaticResource ModernTextBoxStyle}"/>
                        </StackPanel>

                        <!-- Categoría -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Categoría:" FontWeight="SemiBold" Foreground="#495057" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cbocategoria" 
                                      Height="40" 
                                      Style="{StaticResource ModernComboBoxStyle}"/>
                        </StackPanel>

                        <!-- Estado -->
                        <StackPanel Margin="0,0,0,25">
                            <TextBlock Text="Estado:" FontWeight="SemiBold" Foreground="#495057" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cboestado" 
                                      Height="40" 
                                      Style="{StaticResource ModernComboBoxStyle}"/>
                        </StackPanel>

                        <!-- Buttons -->
                        <StackPanel>
                            <Button x:Name="btnguardar" 
                                    Content="💾 Guardar" 
                                    Height="45" 
                                    FontSize="14"
                                    Margin="0,0,0,10"
                                    Click="btnguardar_Click"
                                    Style="{StaticResource ActionButtonStyle}"/>

                            <Button x:Name="btnlimpiar" 
                                    Content="🧹 Limpiar" 
                                    Background="#6C757D"
                                    Height="45" 
                                    FontSize="14"
                                    Margin="0,0,0,10"
                                    Click="btnlimpiar_Click"
                                    Style="{StaticResource ActionButtonStyle}"/>

                            <Button x:Name="btneliminar" 
                                    Content="🗑 Eliminar" 
                                    Background="#DC3545"
                                    Height="45" 
                                    FontSize="14"
                                    Click="btneliminar_Click"
                                    Style="{StaticResource ActionButtonStyle}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right Panel - Products List -->
            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="25" 
                    BorderBrush="#E9ECEF" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Title -->
                    <TextBlock Grid.Row="0" 
                               Text="📋 Lista de Productos" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="#495057" 
                               Margin="0,0,0,20"/>

                    <!-- Search Section -->
                    <Grid Grid.Row="1" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" 
                                   Text="Buscar por:" 
                                   FontWeight="SemiBold" 
                                   Foreground="#495057" 
                                   VerticalAlignment="Center"/>

                        <ComboBox Grid.Column="1" 
                                  x:Name="cbobusqueda" 
                                  Height="35" 
                                  Padding="10,6" 
                                  FontSize="13"
                                  BorderBrush="#DEE2E6" 
                                  BorderThickness="1"/>

                        <TextBox Grid.Column="3" 
                                 x:Name="txtbusqueda" 
                                 Height="35" 
                                 Padding="10" 
                                 FontSize="13"
                                 BorderBrush="#DEE2E6" 
                                 BorderThickness="1"
                                 VerticalContentAlignment="Center"/>

                        <Button Grid.Column="5" 
                                x:Name="btnbuscar"
                                Content="🔍" 
                                Width="35" 
                                Height="35" 
                                FontSize="14"
                                Click="btnbuscar_Click"
                                Style="{StaticResource RoundButtonStyle}"/>

                        <Button Grid.Column="7" 
                                x:Name="btnlimpiarbuscador"
                                Content="🧹" 
                                Width="35" 
                                Height="35" 
                                Background="#6C757D"
                                FontSize="14"
                                Click="btnlimpiarbuscador_Click"
                                Style="{StaticResource RoundButtonStyle}"/>

                        <Button Grid.Column="9" 
                                x:Name="btnexportar"
                                Content="📥 Exportar Excel" 
                                Height="35" 
                                Padding="12,8" 
                                FontWeight="Bold"
                                FontSize="13"
                                Click="btnexportar_Click"
                                Style="{StaticResource ActionButtonStyle}"/>
                    </Grid>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="2" 
                              x:Name="dgvdata" 
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserReorderColumns="False"
                              CanUserResizeRows="False"
                              CanUserResizeColumns="True"
                              SelectionMode="Single"
                              GridLinesVisibility="None"
                              HeadersVisibility="Column"
                              Background="White"
                              BorderThickness="0"
                              RowBackground="White"
                              AlternatingRowBackground="#F8F9FA"
                              FontSize="13"
                              RowHeight="35"
                              ColumnWidth="*"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Auto">

                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Seleccionar" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="✓" 
                                                Background="#28A745" 
                                                Foreground="White" 
                                                BorderThickness="0" 
                                                Width="30" 
                                                Height="30"
                                                FontWeight="Bold"
                                                Cursor="Hand"
                                                Click="btnseleccionar_Click"
                                                Style="{StaticResource RoundButtonStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="0" IsReadOnly="True" Visibility="Collapsed"/>
                            <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="200" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="180" IsReadOnly="True"/>
                            <DataGridTextColumn Header="IdCategoria" Binding="{Binding IdCategoria}" Width="0" IsReadOnly="True" Visibility="Collapsed"/>
                            <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="130" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="80" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Precio Compra" Binding="{Binding PrecioCompra, StringFormat=C}" Width="130" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Precio Venta" Binding="{Binding PrecioVenta, StringFormat=C}" Width="130" IsReadOnly="True"/>
                            <DataGridTextColumn Header="EstadoValor" Binding="{Binding EstadoValor}" Width="0" IsReadOnly="True" Visibility="Collapsed"/>
                            <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100" IsReadOnly="True"/>
                        </DataGrid.Columns>

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F8F9FA"/>
                                <Setter Property="Foreground" Value="#495057"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderBrush" Value="#DEE2E6"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            </Style>
                        </DataGrid.CellStyle>
                    </DataGrid>

                    <!-- Summary Section -->
                    <Border Grid.Row="3" 
                            Background="#F8F9FA" 
                            CornerRadius="4" 
                            Padding="15" 
                            Margin="0,15,0,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="📊 Total de productos: " 
                                       FontWeight="Bold" 
                                       Foreground="#495057" 
                                       FontSize="14"/>
                            <TextBlock x:Name="lblTotalProductos" 
                                       Text="0" 
                                       FontWeight="Bold" 
                                       Foreground="#007BFF" 
                                       FontSize="16"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>